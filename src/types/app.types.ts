export interface SelectOption {
  text: string;
  value: any;
}

export interface MenuPositions {
  top?: string;
  left?: string;
  bottom?: string;
  right?: string;
}

export interface MenuSize {
  height: number | null;
  width: number | null;
}
